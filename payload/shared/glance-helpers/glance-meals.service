[Unit]
Description=Compute current meal-plan day for Glance dashboard
# Runs once at boot before Glance so the widget JSON always exists.
Before=glance.service

[Service]
Type=oneshot
User=glance
Group=glance
ExecStart=/usr/local/bin/glance-meals

[Install]
WantedBy=multi-user.target
